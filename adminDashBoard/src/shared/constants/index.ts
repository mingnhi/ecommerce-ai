export const STORAGE_KEYS = {
  accessToken: "access_token",
  refreshToken: "refresh_token",
  authUser: "auth_user",
} as const;

export const CART_CONSTANTS = {
  TAX_RATE: 0.08,
  FREE_SHIPPING_THRESHOLD: 100,
  SHIPPING_COST: 10,
  SHIPPING_COST_DISPLAY: '$10.00',
} as const;